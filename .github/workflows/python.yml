name: Python CI v2

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4   # Node.js 22 compatible

    - name: Set up Python
      uses: actions/setup-python@v5   # Node.js 22 compatible
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # ---------------------------------------------------------
    # Inject fake API keys so LLMRouter can initialize in CI
    # ---------------------------------------------------------
    - name: Set fake API keys for tests
      run: |
        echo "DEEPSEEK_API_KEY=fake" >> $GITHUB_ENV
        echo "OPENAI_API_KEY=fake" >> $GITHUB_ENV
        echo "GEMINI_API_KEY=fake" >> $GITHUB_ENV

    - name: Run tests
      run: |
        pytest -q

# ============================================================
# FZQ‑AI Task-Level Model Overrides (V15-Final)
# ============================================================

# ------------------------------------------------------------
# Global Model Priority
# ------------------------------------------------------------
model_priority:
  order:
    - deepseek
    - qwen
    - glm
    - openai
    - gemini

# ------------------------------------------------------------
# Task-Level Overrides
# ------------------------------------------------------------
task_overrides:

  # 风险类任务 → DeepSeek
  zh_risk_scan:
    primary: deepseek

  # 政策类任务 → DeepSeek
  zh_policy_brief:
    primary: deepseek

  # 舆论类任务 → Qwen
  zh_opinion_landscape:
    primary: qwen

  # 多源合并 → GLM
  zh_multisource_merge:
    primary: glm

  # 新闻类任务 → GLM
  news_intel:
    primary: glm

  # 长文本任务 → Kimi
  daily_exec_overview:
    primary: kimi

# ------------------------------------------------------------
# Refinement Settings
# ------------------------------------------------------------
refinement:
  enabled: true
  tasks:
    - zh_risk_scan
    - zh_policy_brief
    - zh_opinion_landscape
    - zh_multisource_merge
    - daily_exec_overview
